# Default base env — used when no other env file overrides it
# Points to the deployed Render backend so the app works
# even when running the frontend locally without starting the backend
REACT_APP_API_URL=https://finance-management-system-backend-llzz.onrender.com
