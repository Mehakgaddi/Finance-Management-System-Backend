node_modules/
build/
dist/

# .env.local has local config — never commit it
.env.local

# .env and .env.production are safe to commit — they only contain the public backend URL
# .env          → base default (Render URL)
# .env.production → used by Vercel build (Render URL)
